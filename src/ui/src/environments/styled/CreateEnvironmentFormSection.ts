import styled from 'styled-components';
import { themeHelper } from '../../styled';

export const CreateEnvironmentFormSection = styled.div`
  margin: 0;
  width: 100%;
  border-bottom: ${themeHelper('borders.tiny.border')};
`;
