import { Canvas, Meta, Story } from '@storybook/addon-docs';
import {
  CodeLocation,
  bookMarks,
  getDisabledProps,
  getStandardStoryPath
} from '../../../utils/storybookUtil';
import Card from '../Card';

export const disabledNames = ['data-test'];

<Meta
  title={getStandardStoryPath('Content Containers/Card')}
  component={Card}
  argTypes={{
    type: {
      description: 'Card component'
    },
    ...getDisabledProps(disabledNames)
  }}
/>

## Card
A card can contain any set of related content. It is generally used to represent an object (a project, a workspace, a data source) when the object is present inside another page or when there is a list of that object type.

export const Template = (args) => <Card {...args} />

<Canvas>
  <Story
    name="Card"
    args={{
      title: 'Card Title',
      showTitleSeparator: false,
      children: 'Placeholder Card description. Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
      width: 300,
      useChildren: false,
      useWrapper: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Usage Guidelines
No notes.

Configuration:
No notes.

### Accessibility
No notes.

### Technical Notes
<CodeLocation.Card/>

### Composition
Uses:
* Text
* Colors
* Icon
* Can contain any other component as content

Used in:
* Any
