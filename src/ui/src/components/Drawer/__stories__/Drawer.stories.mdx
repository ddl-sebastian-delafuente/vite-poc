import { Canvas, Meta, Story } from '@storybook/addon-docs';
import {
  CodeLocation,
  bookMarks,
  getDisabledProps,
  getStandardStoryPath
} from '../../../utils/storybookUtil';
import Drawer from '../Drawer';

export const disabledNames = [
  'dataTest',
  'type'
];

<Meta
  title={getStandardStoryPath('Content Containers/Drawer')}
  component={Drawer}
  argTypes={{
    type: {
      description: 'Drawer component'
    },
    ...getDisabledProps(disabledNames)
  }}
/>

## Drawer
A drawer can contain any set of related content. It is generally used to represent additional details or the successive steps of current action

export const Template = (args) => <Drawer {...args} />

<Canvas>
  <Story
    name="Drawer"
    args={{
      title: 'Edit Permissions: GeneralAdmin',
      buttonLabel: 'Open',
      children: 'Drawer content',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Usage Guidelines
No notes.

Configuration:
No notes.

### Accessibility
No notes.

### Technical Notes
<CodeLocation.Card/>

### Composition
Uses:
* Text
* Colors
* Icon
* Can contain any other component as content

Used in:
* Any
